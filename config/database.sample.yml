# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
#
#   Note: don't use SQlite because it does not support the year() and month() function
#   To create a MySQL database, login to MySQL as root and do:
#   grant all privileges on jobtorrent_development.* to jobtorrent@localhost identified by 'bigsecret';
#   create database jobtorrent_development;
#
development:
  adapter: mysql
  database: jobtorrent_development
  username: jobtorrent
  password: 
  timeout: 5000

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql
  database: jobtorrent_test
  username: jobtorrent
  password: 
  timeout: 5000

production:
  adapter: mysql
  database: jobtorrent
  username: jobtorrent
  password: 
  timeout: 5000
